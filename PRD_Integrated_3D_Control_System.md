# 이기종 장비 실시간 3D 통합관제시스템 PRD

## 1. 문서 개요

### 1.1 목적

본 문서는 AGV, AMR, ABCS 등 이기종 물류/자동화 장비의 실시간 상태를 3D 공간에서 통합적으로 표현하고 관제할 수 있는 통합관제시스템의 제품 요구사항을 정의한다. 시스템은 현장 운영자가 장비 위치, 상태, 작업 흐름, 장애 상황을 직관적으로 파악하고 신속하게 대응할 수 있도록 지원한다.

### 1.2 대상 사용자

- 관제 운영자: 실시간 장비 상태 모니터링, 알람 확인, 작업 흐름 파악
- 현장 관리자: 생산성, 병목, 장애 이력, 장비 가동률 확인
- 유지보수 담당자: 장애 장비 식별, 원인 추적, 정비 이력 확인
- 시스템 관리자: 장비 연동 설정, 권한 관리, 맵/레이아웃 관리
- 경영/운영 책임자: 운영 KPI, 설비 효율, 장애 대응 현황 확인

### 1.3 주요 용어

| 용어 | 설명 |
| --- | --- |
| AGV | Automated Guided Vehicle. 정해진 경로를 따라 이동하는 무인 운반 장비 |
| AMR | Autonomous Mobile Robot. 자율주행 기반 이동 로봇 |
| ABCS | Automated Box/Buffer/Conveyor System 등 현장 정의에 따른 자동화 설비군 |
| Digital Twin | 실제 현장, 장비, 작업 상태를 디지털 공간에 실시간 또는 준실시간으로 복제한 모델 |
| 관제 | 장비 상태, 위치, 작업, 알람을 모니터링하고 운영 의사결정을 지원하는 행위 |

## 2. 배경 및 문제 정의

### 2.1 현황

물류센터, 제조라인, 스마트팩토리 환경에서는 AGV, AMR, 컨베이어, 자동창고, 분류기, 리프트, 로봇암 등 다양한 장비가 동시에 운영된다. 장비별 제어 시스템과 모니터링 화면이 분리되어 있으면 운영자는 전체 흐름을 한눈에 파악하기 어렵고, 장애 발생 시 원인과 영향 범위를 빠르게 판단하기 어렵다.

### 2.2 해결해야 할 문제

- 장비 제조사와 프로토콜이 달라 통합 관제가 어렵다.
- 2D 화면만으로는 장비 위치, 층간 이동, 설비 간 관계를 직관적으로 이해하기 어렵다.
- 장애가 발생해도 해당 장비가 전체 작업 흐름에 미치는 영향을 즉시 파악하기 어렵다.
- 실시간 상태 데이터와 이력 데이터가 분리되어 운영 분석이 어렵다.
- 운영자, 관리자, 유지보수 담당자별로 필요한 정보 수준이 다르지만 화면이 세분화되어 있지 않다.

## 3. 제품 목표

### 3.1 비즈니스 목표

- 이기종 장비를 단일 관제 화면에서 통합 모니터링한다.
- 장비 장애 인지 및 초동 대응 시간을 단축한다.
- 현장 작업 흐름의 병목 구간과 비효율을 시각적으로 파악한다.
- 장비 가동률, 작업 처리량, 장애 빈도 등 운영 KPI를 개선한다.
- 향후 신규 장비와 신규 구역을 쉽게 추가할 수 있는 확장 가능한 관제 기반을 구축한다.

### 3.2 사용자 목표

- 운영자는 실시간 장비 위치와 상태를 3D 맵에서 확인할 수 있다.
- 장애 또는 위험 상황을 색상, 아이콘, 알림, 팝업으로 즉시 인지할 수 있다.
- 특정 장비를 선택해 상세 상태, 현재 작업, 배터리, 속도, 목적지, 알람 이력을 확인할 수 있다.
- 관리자는 기간별 운영 지표와 이력 데이터를 조회할 수 있다.
- 시스템 관리자는 장비 연동, 맵, 권한, 알림 정책을 설정할 수 있다.

### 3.3 성공 지표

| 지표 | 목표 |
| --- | --- |
| 실시간 위치 반영 지연 | 평균 1초 이내, 최대 3초 이내 |
| 주요 장비 상태 반영 지연 | 평균 1초 이내 |
| 장애 알림 발생 지연 | 이벤트 수신 후 2초 이내 |
| 관제 화면 초기 로딩 | 표준 현장 기준 5초 이내 |
| 동시 표시 장비 수 | 500대 이상 확장 가능 |
| 장애 탐지 후 운영자 확인 시간 | 기존 대비 50% 이상 단축 |
| 시스템 가용성 | 월 99.5% 이상 |

## 4. 제품 범위

### 4.1 포함 범위

- 3D 기반 현장 맵 표시
- AGV, AMR, ABCS 등 이기종 장비의 실시간 상태 통합 표시
- 장비 위치, 방향, 이동 경로, 작업 상태 시각화
- 장비 상세 정보 패널
- 알람 및 이벤트 모니터링
- 장비 검색, 필터, 그룹화
- 작업 흐름 및 경로 표시
- 운영 KPI 대시보드
- 이력 조회 및 리플레이
- 사용자 권한 관리
- 장비 연동 API 및 데이터 표준화 계층

### 4.2 제외 범위

- 장비 자체의 주행 알고리즘 개발
- 장비 펌웨어 개발
- WMS, MES, MCS의 전체 기능 대체
- 물리 장비 제어권의 직접 인수
- CAD/BIM 저작도구 수준의 3D 모델링 기능

단, 외부 시스템과의 연동 및 제한적 제어 명령 전달은 시스템 정책에 따라 포함할 수 있다.

## 5. 사용자 시나리오

### 5.1 실시간 관제

운영자는 관제 화면에 접속해 전체 현장의 3D 맵을 확인한다. 각 장비는 현재 위치와 상태에 따라 색상과 아이콘으로 표시된다. 운영자는 특정 구역을 확대하거나 특정 장비를 검색해 현재 작업, 목적지, 배터리, 속도, 장애 여부를 확인한다.

### 5.2 장애 대응

AMR 한 대가 경로 상에서 정지하고 장애 이벤트를 발생시킨다. 시스템은 해당 장비를 빨간색으로 강조하고 알람 패널에 이벤트를 표시한다. 운영자는 장비를 클릭해 오류 코드, 위치, 마지막 작업, 주변 장비, 영향받는 경로를 확인하고 유지보수 담당자에게 조치를 요청한다.

### 5.3 병목 분석

관리자는 지난 24시간 동안 특정 구역의 장비 밀집도와 대기 시간을 조회한다. 시스템은 히트맵과 통계 차트를 통해 병목 구간을 표시하고, 특정 시간대의 장비 이동 리플레이를 제공한다.

### 5.4 유지보수 추적

유지보수 담당자는 장애 빈도가 높은 장비 목록을 확인한다. 특정 장비를 선택하면 최근 알람 이력, 누적 주행 거리, 배터리 상태, 정비 이력을 확인할 수 있다.

## 6. 기능 요구사항

### 6.1 3D 맵 및 디지털 트윈 화면

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-001 | 현장 레이아웃을 3D 모델로 표시해야 한다. | Must |
| F-002 | 층, 구역, 랙, 컨베이어, 도어, 충전소, 작업 스테이션 등 주요 오브젝트를 표시해야 한다. | Must |
| F-003 | 사용자는 확대, 축소, 회전, 이동, 시점 초기화를 수행할 수 있어야 한다. | Must |
| F-004 | 2D 탑뷰와 3D 뷰를 전환할 수 있어야 한다. | Should |
| F-005 | 구역별 표시/숨김, 투명도 조절, 레이어 제어를 제공해야 한다. | Should |
| F-006 | 대형 현장에서도 렌더링 성능을 유지하기 위해 LOD 또는 객체 최적화를 지원해야 한다. | Must |

### 6.2 장비 실시간 표시

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-101 | AGV, AMR, ABCS 등 장비 유형별 3D 모델 또는 아이콘을 구분해 표시해야 한다. | Must |
| F-102 | 장비의 현재 위치, 방향, 속도, 상태를 실시간으로 갱신해야 한다. | Must |
| F-103 | 장비 상태는 색상 또는 상태 배지로 구분해야 한다. | Must |
| F-104 | 장비의 현재 경로, 목적지, 작업 구간을 표시할 수 있어야 한다. | Must |
| F-105 | 장비가 통신 두절 상태일 경우 마지막 수신 위치와 수신 시간을 표시해야 한다. | Must |
| F-106 | 충전 중, 작업 중, 대기 중, 오류, 수동 조작, 긴급정지 상태를 구분해야 한다. | Must |

### 6.3 장비 상세 패널

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-201 | 장비 클릭 시 상세 패널을 표시해야 한다. | Must |
| F-202 | 상세 패널에는 장비 ID, 유형, 제조사, 상태, 위치, 속도, 배터리, 현재 작업, 목적지, 최근 이벤트가 포함되어야 한다. | Must |
| F-203 | AMR/AGV는 배터리, 주행 거리, 적재 상태, 충전 필요 여부를 표시해야 한다. | Must |
| F-204 | ABCS 설비는 동작 상태, 처리량, 센서 상태, 막힘/정지 여부를 표시해야 한다. | Must |
| F-205 | 장비별 알람 이력과 작업 이력을 조회할 수 있어야 한다. | Should |

### 6.4 알람 및 이벤트

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-301 | 장애, 경고, 정보 이벤트를 심각도별로 구분해야 한다. | Must |
| F-302 | 알람 발생 시 3D 화면에서 해당 장비 또는 구역을 강조해야 한다. | Must |
| F-303 | 알람 목록은 발생 시간, 장비, 구역, 심각도, 메시지, 조치 상태를 포함해야 한다. | Must |
| F-304 | 운영자는 알람을 확인, 조치 중, 완료 상태로 변경할 수 있어야 한다. | Should |
| F-305 | 동일 유형 알람의 반복 발생을 그룹화할 수 있어야 한다. | Should |
| F-306 | 알람 조건과 수신 대상은 설정 화면에서 관리할 수 있어야 한다. | Could |

### 6.5 검색, 필터, 그룹화

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-401 | 장비 ID, 유형, 상태, 구역, 작업 ID로 검색할 수 있어야 한다. | Must |
| F-402 | 장비 유형별, 상태별, 구역별 필터를 제공해야 한다. | Must |
| F-403 | 특정 장비 선택 시 카메라가 해당 장비 위치로 이동해야 한다. | Must |
| F-404 | 즐겨찾기 장비 또는 관심 구역을 저장할 수 있어야 한다. | Should |

### 6.6 운영 KPI 대시보드

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-501 | 전체 장비 수, 정상/경고/장애/통신두절 수를 표시해야 한다. | Must |
| F-502 | 장비 가동률, 작업 처리량, 평균 대기 시간, 장애 발생 건수를 표시해야 한다. | Must |
| F-503 | 구역별 혼잡도와 장비 밀집도를 표시해야 한다. | Should |
| F-504 | 시간대별 작업량과 장애 추이를 차트로 제공해야 한다. | Should |
| F-505 | KPI 데이터는 CSV 또는 Excel로 내보낼 수 있어야 한다. | Could |

### 6.7 이력 조회 및 리플레이

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-601 | 특정 시간 범위의 장비 위치 및 상태 이력을 조회할 수 있어야 한다. | Must |
| F-602 | 선택한 시간 범위를 3D 맵에서 리플레이할 수 있어야 한다. | Should |
| F-603 | 리플레이 속도 조절, 일시정지, 특정 시점 이동을 지원해야 한다. | Should |
| F-604 | 알람 발생 시점을 기준으로 전후 장비 움직임을 확인할 수 있어야 한다. | Should |

### 6.8 연동 및 데이터 수집

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-701 | 장비별 프로토콜 차이를 흡수하는 데이터 어댑터 구조를 제공해야 한다. | Must |
| F-702 | MQTT, WebSocket, REST API, OPC UA 등 주요 연동 방식을 지원할 수 있어야 한다. | Must |
| F-703 | 수집 데이터는 표준 장비 상태 모델로 정규화되어야 한다. | Must |
| F-704 | 외부 WMS, MES, MCS, RMS와 작업 ID 및 장비 상태를 연동할 수 있어야 한다. | Should |
| F-705 | 데이터 수신 실패, 지연, 이상값을 감지하고 로그를 남겨야 한다. | Must |

### 6.9 사용자 및 권한

| ID | 요구사항 | 우선순위 |
| --- | --- | --- |
| F-801 | 사용자 로그인과 역할 기반 권한 관리를 제공해야 한다. | Must |
| F-802 | 운영자, 관리자, 유지보수, 시스템 관리자 역할을 구분해야 한다. | Must |
| F-803 | 메뉴, 설정, 알람 조치, 데이터 내보내기 권한을 역할별로 제어해야 한다. | Should |
| F-804 | 주요 사용자 행위는 감사 로그로 기록해야 한다. | Should |

## 7. 데이터 요구사항

### 7.1 표준 장비 상태 모델

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| equipmentId | string | 장비 고유 ID |
| equipmentType | enum | AGV, AMR, ABCS, Conveyor, Lift 등 |
| vendor | string | 제조사 |
| status | enum | RUNNING, IDLE, CHARGING, WARNING, ERROR, OFFLINE, MANUAL, E_STOP |
| position.x | number | 현장 좌표 X |
| position.y | number | 현장 좌표 Y |
| position.z | number | 현장 좌표 Z |
| heading | number | 장비 방향 |
| speed | number | 현재 속도 |
| batteryLevel | number | 배터리 잔량 |
| taskId | string | 현재 작업 ID |
| source | string | 출발지 |
| destination | string | 목적지 |
| payloadStatus | enum | EMPTY, LOADED, UNKNOWN |
| alarmCode | string | 현재 알람 코드 |
| timestamp | datetime | 데이터 발생 시각 |
| receivedAt | datetime | 서버 수신 시각 |

### 7.2 이벤트 모델

| 필드 | 타입 | 설명 |
| --- | --- | --- |
| eventId | string | 이벤트 고유 ID |
| eventType | enum | INFO, WARNING, ERROR, RECOVERY, STATE_CHANGE |
| severity | enum | LOW, MEDIUM, HIGH, CRITICAL |
| equipmentId | string | 관련 장비 ID |
| zoneId | string | 관련 구역 ID |
| message | string | 이벤트 메시지 |
| code | string | 이벤트 코드 |
| occurredAt | datetime | 발생 시각 |
| acknowledgedBy | string | 확인 사용자 |
| acknowledgedAt | datetime | 확인 시각 |
| resolvedAt | datetime | 해결 시각 |

## 8. 화면 요구사항

### 8.1 메인 관제 화면

- 좌측: 장비 목록, 검색, 필터
- 중앙: 3D 현장 맵
- 우측: 선택 장비 상세 정보 또는 알람 상세
- 하단: 주요 KPI, 시간축, 리플레이 컨트롤
- 상단: 현장 선택, 뷰 전환, 알림, 사용자 메뉴

### 8.2 알람 화면

- 실시간 알람 목록
- 심각도별 필터
- 장비/구역/시간별 검색
- 알람 상세 및 조치 상태 변경
- 반복 알람 및 미조치 알람 강조

### 8.3 분석 대시보드

- 장비 가동률
- 작업 처리량
- 장애 발생 추이
- 구역별 혼잡도
- 장비별 장애 Top N
- 시간대별 병목 분석

### 8.4 설정 화면

- 장비 등록 및 매핑
- 장비 유형별 표시 모델 설정
- 맵/구역/레이어 관리
- 알람 정책 설정
- 사용자 및 권한 관리
- 외부 시스템 연동 설정

## 9. 비기능 요구사항

### 9.1 성능

- 일반 관제 화면은 500대 이상의 장비 상태를 표시할 수 있어야 한다.
- 위치 데이터는 초당 1회 이상 갱신 가능해야 한다.
- 데이터 수신량 증가에 대비해 메시지 브로커와 스트리밍 처리를 고려해야 한다.
- 3D 렌더링은 표준 운영 PC에서 30 FPS 이상을 목표로 한다.
- 장비 수가 많은 경우 클러스터링, LOD, 인스턴싱 렌더링을 적용해야 한다.

### 9.2 안정성

- 관제 서버 장애 시 데이터 손실을 최소화해야 한다.
- 장비 데이터 수집 모듈은 자동 재연결을 지원해야 한다.
- 통신 두절 장비는 명확히 표시하고 마지막 상태를 보존해야 한다.
- 주요 설정 변경은 이력과 롤백 가능성을 고려해야 한다.

### 9.3 보안

- 모든 사용자 접근은 인증을 거쳐야 한다.
- 외부 시스템 연동 API는 인증 토큰 또는 인증서를 사용해야 한다.
- 역할 기반 접근 제어를 적용해야 한다.
- 감사 로그는 위변조 방지를 고려해 저장해야 한다.
- 개인정보가 포함될 경우 관련 법규에 따라 암호화 및 접근 제한을 적용해야 한다.

### 9.4 확장성

- 신규 장비 유형 추가 시 기존 관제 화면을 크게 수정하지 않아야 한다.
- 장비 어댑터는 플러그인 또는 모듈 방식으로 확장 가능해야 한다.
- 멀티 사이트 운영을 고려해 사이트, 건물, 층, 구역 단위의 구조를 지원해야 한다.
- 실시간 처리 계층과 이력 분석 계층은 독립적으로 확장 가능해야 한다.

### 9.5 사용성

- 장애 상태는 운영자가 즉시 인지할 수 있도록 색상, 아이콘, 알림을 함께 사용한다.
- 색상만으로 상태를 구분하지 않고 텍스트 또는 아이콘을 병행한다.
- 주요 작업은 3회 이하의 클릭으로 접근 가능해야 한다.
- 관제 화면은 장시간 모니터링에 적합하도록 과도한 애니메이션과 시각적 노이즈를 제한한다.

## 10. 시스템 아키텍처 요구사항

### 10.1 논리 구성

- 장비 연동 계층: AGV, AMR, ABCS, 센서, 외부 시스템 데이터 수집
- 데이터 정규화 계층: 장비별 데이터를 표준 상태 모델로 변환
- 실시간 메시징 계층: 상태 변경, 위치, 알람 이벤트 전달
- 저장 계층: 실시간 상태, 이벤트, 이력, 설정 데이터 저장
- API 계층: 관제 UI, 외부 시스템, 관리자 기능 제공
- 3D 클라이언트: 디지털 트윈 시각화 및 사용자 상호작용
- 분석 계층: KPI, 통계, 리플레이, 병목 분석

### 10.2 권장 기술 방향

| 영역 | 권장 방향 |
| --- | --- |
| 실시간 통신 | WebSocket 또는 MQTT |
| 장비 연동 | MQTT, REST, OPC UA, TCP Socket 어댑터 |
| 3D 렌더링 | WebGL 기반 Three.js 또는 Babylon.js |
| 백엔드 API | REST 및 이벤트 스트림 병행 |
| 시계열 데이터 | TimescaleDB, InfluxDB 또는 유사 시계열 저장소 |
| 이벤트 처리 | Kafka, RabbitMQ, Redis Streams 등 |
| 운영 대시보드 | 웹 기반 반응형 UI |

## 11. 정책 및 상태 정의

### 11.1 장비 상태

| 상태 | 표시 색상 예시 | 설명 |
| --- | --- | --- |
| RUNNING | Green | 정상 작업 중 |
| IDLE | Blue | 대기 중 |
| CHARGING | Cyan | 충전 중 |
| WARNING | Yellow | 경고 발생 |
| ERROR | Red | 장애 발생 |
| OFFLINE | Gray | 통신 두절 |
| MANUAL | Purple | 수동 조작 중 |
| E_STOP | Red Flash | 긴급정지 |

### 11.2 알람 심각도

| 심각도 | 설명 | 예시 |
| --- | --- | --- |
| CRITICAL | 즉시 조치가 필요한 장애 | 긴급정지, 충돌 위험, 화재/안전 이벤트 |
| HIGH | 운영에 큰 영향을 주는 장애 | 장비 정지, 경로 차단, 주요 설비 오류 |
| MEDIUM | 조치가 필요한 경고 | 배터리 부족, 센서 이상, 반복 지연 |
| LOW | 참고용 정보 또는 경미한 경고 | 일시적 통신 지연, 작업 지연 |

## 12. MVP 범위

### 12.1 MVP 필수 기능

- 3D 현장 맵 표시
- AGV, AMR, ABCS 기본 상태 실시간 표시
- 장비 위치, 방향, 상태, 배터리, 작업 ID 표시
- 장비 검색 및 상태별 필터
- 장비 상세 패널
- 실시간 알람 목록
- 알람 발생 장비 강조
- 기본 KPI 표시
- 장비 상태 표준 모델 및 API
- 관리자용 장비 등록/매핑 기능

### 12.2 MVP 제외 가능 기능

- 고급 리플레이
- 병목 히트맵
- 복잡한 권한 정책
- 예측 정비
- 자동 원인 분석
- 모바일 전용 UI
- 다중 사이트 통합 대시보드

## 13. 릴리즈 계획

### Phase 1: MVP

- 기본 3D 관제 화면
- 장비 상태 실시간 표시
- 장비 상세 조회
- 알람 목록
- 기본 장비 연동 어댑터
- 기본 KPI

### Phase 2: 운영 고도화

- 이력 조회 및 리플레이
- 구역별 혼잡도
- 알람 조치 워크플로우
- 권한 관리
- 외부 WMS/MES/MCS 연동 확대

### Phase 3: 분석 및 최적화

- 병목 분석
- 장비 가동률 분석
- 예측 정비 지표
- 다중 사이트 통합
- 운영 리포트 자동 생성

## 14. 리스크 및 대응 방안

| 리스크 | 영향 | 대응 방안 |
| --- | --- | --- |
| 장비별 프로토콜 상이 | 연동 일정 지연 | 어댑터 기반 구조와 표준 상태 모델 우선 정의 |
| 위치 좌표계 불일치 | 3D 맵 위치 오류 | 좌표 변환 규칙과 캘리브레이션 도구 제공 |
| 데이터 지연 또는 유실 | 관제 신뢰도 저하 | 수신 시간 표시, 지연 감지, 재연결, 버퍼링 적용 |
| 3D 성능 저하 | 사용성 저하 | LOD, 인스턴싱, 표시 레이어 제어 적용 |
| 현장 맵 변경 빈도 높음 | 유지보수 비용 증가 | 맵/구역 설정 관리 기능 제공 |
| 운영자 정보 과부하 | 장애 인지 지연 | 중요도 기반 알림, 필터, 화면 단순화 적용 |

## 15. 의존성

- 장비별 상태 데이터 제공 가능 여부
- 현장 좌표계 및 3D 맵 데이터 확보
- 외부 시스템 연동 API 제공 여부
- 네트워크 품질 및 보안 정책
- 장비 제조사별 알람 코드 정의서
- 운영 조직의 알람 처리 프로세스

## 16. 오픈 이슈

- ABCS의 정확한 장비 범위와 상태 데이터 항목 정의 필요
- 장비 제어 명령을 관제시스템에서 허용할지 여부 결정 필요
- 현장 3D 모델의 원천 데이터 형식 확정 필요
- 실시간 위치 갱신 주기와 장비별 데이터 송신 주기 확인 필요
- 장애 알림 수신 채널을 웹 화면으로 한정할지, SMS/메신저/이메일까지 확장할지 결정 필요
- 온프레미스, 클라우드, 하이브리드 배포 방식 결정 필요

## 17. 승인 기준

본 PRD는 다음 조건이 충족될 때 승인 대상으로 본다.

- MVP 기능 범위가 이해관계자 간 합의되었다.
- 장비별 필수 데이터 항목과 연동 방식이 정의되었다.
- 3D 맵 및 좌표계 기준이 확정되었다.
- 성능, 보안, 가용성 목표가 운영 환경 기준으로 검토되었다.
- 릴리즈 단계와 우선순위가 확정되었다.

